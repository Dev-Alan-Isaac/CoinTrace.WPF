<Window x:Class="Smart_Tools.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign in" Height="330" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <StackPanel Margin="24">
        <TextBlock Text="Username" Margin="0,0,0,4"/>
        <TextBox x:Name="UsernameBox" Padding="6"/>

        <TextBlock Text="Password" Margin="0,16,0,4"/>
        <PasswordBox x:Name="PasswordBox" Padding="6"/>

        <TextBlock x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap"
                   Margin="0,12,0,0" Visibility="Collapsed"/>

        <Grid Margin="0,20,0,0">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left">
                <Hyperlink x:Name="ForgotPasswordLink" Click="ForgotPasswordLink_Click">
                    Forgot password?
                </Hyperlink>
            </TextBlock>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" Content="Cancel" Width="80" Margin="0,0,8,0"
                        Click="CancelButton_Click"/>
                <Button x:Name="LoginButton" Content="Sign in" Width="80" IsDefault="True"
                        Click="LoginButton_Click"/>
            </StackPanel>
        </Grid>
    </StackPanel>
</Window>
